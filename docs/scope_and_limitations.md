# Scope and Limitations

This project does not implement a quantum attack, recover private keys, or verify withheld circuits from prior work.

The initial scope is public reversible arithmetic-block certification.

Randomized deterministic testing is probabilistic unless the test domain is exhausted or the circuit is formally verified. Future versions may add formal proof artifacts or zero-knowledge proof prototypes.
